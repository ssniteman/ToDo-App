/* global describe, it */

(function () {
    'use strict';

    describe('To-do List', function () {
        describe('when the add button is clicked', function () {
            it('a new div should populate when a input is rendered', function () {

            });
        });
    });
})();
